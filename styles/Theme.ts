import { extendTheme } from '@chakra-ui/react'

const configTheme = extendTheme({
  fonts: {
    heading: 'Work Sans',
    body: 'Work Sans',
    code: 'Inconsolata'
  }
})

export default configTheme